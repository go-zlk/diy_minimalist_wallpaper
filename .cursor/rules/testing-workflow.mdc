---
description: 测试流程规范 - 新增功能需加测试，修改代码需跑测试
alwaysApply: true
---

# WallpaperDIY 测试流程规范

## 核心原则

1. **新增功能必须增加测试**：每增加一个功能点，需在 `tests/` 下新增或补充对应测试用例
2. **修改代码必须跑测试**：任何对 `main.py` 或核心逻辑的修改，需先运行测试，通过后再继续
3. **测试通过是继续的前提**：测试失败时，应先修复测试或修复代码，不得跳过

## 运行测试

```bash
# 安装测试依赖（首次）
pip install -r config/requirements-dev.txt

# 运行全部测试（从项目根目录）
python -m pytest tests/ -v

# 快速运行（简短输出）
python -m pytest tests/ -v --tb=short
```

## 测试结构

- `tests/test_utils.py` - 工具函数（get_output_dir, get_arrow_path）与常量
- `tests/test_render.py` - 渲染逻辑（渐变、文字度量）
- `tests/test_dynamic_vars.py` - 动态变量处理、字体回退

## 新增测试的约定

- 新功能对应新 `test_*.py` 或扩展现有测试类
- 测试函数命名：`test_<功能描述>`
- 使用 `wallpaper_app` fixture 测试依赖 PyQt 的逻辑
- 使用 `temp_output_dir` fixture 测试涉及输出目录的逻辑

## 工作流检查清单

- [ ] 新增功能：已添加对应测试用例
- [ ] 修改代码：已运行 `pytest tests/` 且全部通过
- [ ] 提交前：确认无测试被跳过（除非有明确原因）
